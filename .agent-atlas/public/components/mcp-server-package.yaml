id: component:mcp-server-package
kind: component
title: MCP Server Package
summary: Read-only MCP server exposing atlas resources and traversal tools over stdio.
visibility: public
code:
  paths:
    - packages/mcp-server/**
  entrypoints:
    - packages/mcp-server/src/index.ts
relations:
  - type: part-of
    target: domain:agent-atlas
  - type: implements
    target: workflow:expose-atlas-mcp
  - type: documented-in
    target: document:mcp-spec
  - type: tested-by
    target: test-scope:workspace-build-and-test
