id: component:cli-package
kind: component
title: CLI Package
summary: Provides the atlas command-line interface for validation, boundary checks, graph inspection, path resolution, context packs, generated Markdown checks, incremental card suggestions, policy-driven maintenance, stale-reference diffing, cross-repo registry validation, manifests, global generated Markdown, migrations, benchmarks, setup doctor checks, usage evidence, and the local review UI launcher.
visibility: public
code:
  paths:
    - packages/cli/**
  entrypoints:
    - packages/cli/src/index.ts
relations:
  - type: part-of
    target: domain:agent-atlas
  - type: implements
    target: workflow:operate-atlas-cli
  - type: implements
    target: workflow:create-context-pack
  - type: implements
    target: workflow:manage-overlays
  - type: implements
    target: workflow:audit-profile-boundaries
  - type: implements
    target: workflow:maintain-atlas-metadata
  - type: implements
    target: workflow:measure-atlas-adoption
  - type: implements
    target: workflow:onboard-downstream-repo
  - type: implements
    target: workflow:operate-cross-repo-registry
  - type: uses
    target: component:core-package
  - type: uses
    target: component:markdown-package
  - type: uses
    target: component:ui-package
  - type: documented-in
    target: document:cli-readme
  - type: documented-in
    target: document:cli-command-reference
  - type: documented-in
    target: document:registry-spec
  - type: documented-in
    target: document:versioning-spec
  - type: documented-in
    target: document:usage-evidence-spec
  - type: documented-in
    target: document:boundary-safety-spec
  - type: documented-in
    target: document:boundary-checks-guide
  - type: documented-in
    target: document:rollout-evidence-guide
  - type: documented-in
    target: document:incremental-authoring-guide
  - type: documented-in
    target: document:local-cli-consumption-guide
  - type: documented-in
    target: document:per-repo-setup-guide
  - type: documented-in
    target: document:llm-agent-usage-guide
  - type: documented-in
    target: document:cross-repo-atlas-guide
  - type: tested-by
    target: test-scope:workspace-build-and-test
