id: component:cli-package
kind: component
title: CLI Package
summary: Provides the atlas command-line interface for validation, graph inspection, path resolution, context packs, generated Markdown, and cross-repo registry commands.
visibility: public
code:
  paths:
    - packages/cli/**
  entrypoints:
    - packages/cli/src/index.ts
relations:
  - type: part-of
    target: domain:agent-atlas
  - type: implements
    target: workflow:operate-atlas-cli
  - type: implements
    target: workflow:create-context-pack
  - type: implements
    target: workflow:manage-overlays
  - type: uses
    target: component:core-package
  - type: uses
    target: component:markdown-package
  - type: documented-in
    target: document:cli-readme
  - type: documented-in
    target: document:registry-spec
  - type: tested-by
    target: test-scope:workspace-build-and-test
