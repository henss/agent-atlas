id: component:core-package
kind: component
title: Core Package
summary: Loads atlas YAML, applies selected overlays, validates graph structure, normalizes relations, resolves paths, supports traversal, creates context packs, and merges cross-repo registries.
visibility: public
code:
  paths:
    - packages/core/**
  entrypoints:
    - packages/core/src/index.ts
relations:
  - type: part-of
    target: domain:agent-atlas
  - type: implements
    target: workflow:navigate-atlas-graph
  - type: implements
    target: workflow:create-context-pack
  - type: implements
    target: workflow:manage-overlays
  - type: documented-in
    target: document:registry-spec
  - type: uses
    target: component:schema-package
  - type: tested-by
    target: test-scope:workspace-build-and-test
