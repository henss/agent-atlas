id: workflow:operate-atlas-cli
kind: workflow
title: Operate Atlas CLI
summary: Exposes validation, graph navigation, path resolution, context packs, Markdown generation, migrations, benchmarks, global registry manifests, and global registry workflows through concise CLI commands.
visibility: public
relations:
  - type: part-of
    target: domain:agent-atlas
  - type: uses
    target: component:cli-package
  - type: uses
    target: component:core-package
  - type: documented-in
    target: document:cli-readme
  - type: tested-by
    target: test-scope:workspace-build-and-test
agent:
  load_when:
    - Adding or changing atlas CLI commands
  token_budget_hint: 1200
