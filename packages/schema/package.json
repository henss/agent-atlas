{
  "name": "@agent-atlas/schema",
  "version": "0.15.0",
  "description": "Types and JSON Schema for Agent Atlas entities.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./schema/atlas.entity.schema.json": "./schema/atlas.entity.schema.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
