# @agent-atlas/schema

Types and JSON Schema for atlas entity cards.

Initial implementation tasks:

- finalize `AtlasEntity` TypeScript shape
- implement discriminated entity kinds
- keep JSON Schema in sync
- add tests that validate examples
- define versioning and migrations
