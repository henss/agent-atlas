{
  "name": "@agent-atlas/mcp-server",
  "version": "0.18.0",
  "description": "Read-only MCP server for Agent Atlas.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "bin": {
    "atlas-mcp": "./dist/stdio.js"
  },
  "dependencies": {
    "@agent-atlas/core": "workspace:*",
    "@agent-atlas/schema": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
