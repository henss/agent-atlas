{
  "name": "@agent-atlas/core",
  "version": "0.14.0",
  "description": "Core graph loading, validation, traversal, and context pack logic for Agent Atlas.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "dependencies": {
    "@agent-atlas/schema": "workspace:*",
    "yaml": "^2.6.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
