{
  "name": "@agent-atlas/cli",
  "version": "0.17.0",
  "description": "Command-line interface for Agent Atlas.",
  "license": "Apache-2.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/henss/agent-atlas.git",
    "directory": "packages/cli"
  },
  "bin": {
    "atlas": "dist/index.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "LICENSE",
    "README.md",
    "dist/index.js"
  ],
  "dependencies": {
    "@agent-atlas/ui": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "yaml": "^2.6.0",
    "zod": "^3.25.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "pnpm run build && vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepack": "pnpm run build",
    "atlas": "node dist/index.js"
  }
}
